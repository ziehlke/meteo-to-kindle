[project]
name = "meteo-to-kindle"
version = "0.1.0"
description = "Fetches a meteo.pl forecast, overlays Airly air quality data and formats it for a Kindle e-ink display"
requires-python = ">=3.12"
dependencies = [
    "httpx",
    "pillow",
    "matplotlib",
    "numpy",
    "python-dotenv",
]
